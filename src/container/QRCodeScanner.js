import QRCodeScanner from '../screen/QRCodeScanner';
import { connect } from 'react-redux';
import { } from '../redux/actions';

/*========================================================
    * function Name: QRCodeScanner pages
    * function Purpose: called action and stored state
    * function Parameters: QRCodeScanner, mapStateToProps, dispatchers
    * function ReturnType: state
    * function Description: called action of dispatchers and stored state of mapStateToProps
    *=====================================================*/

const mapStateToProps = state => ({

});

const mapDispatchToProps = {
  
};

export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(QRCodeScanner);